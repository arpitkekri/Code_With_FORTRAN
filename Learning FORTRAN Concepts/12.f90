PROGRAM functions
    IMPLICIT NONE
    
    ! REAL :: x
    ! READ*, x
    ! PRINT*, Negative(x)
    ! CONTAINS
    ! REAL FUNCTION Negative(a)
    !     REAL:: a
    !     Negative = -a
    !     return 
    ! END FUNCTION

    ! REAL:: x, y, z
    ! x = 3
    ! y = 4
    ! z = F(x, y)
    ! print *, z
    ! CONTAINS 
    ! REAL FUNCTION F(x, y)
    !     IMPLICIT NONE
    !     REAL:: x, y
    !     F = sqrt(x*x + y*y)
    ! END FUNCTION
    ! function F(n1, n2) result(sum)
    !     implicit NONE
    !     real :: n1, n2 
    !     real :: sum 
    !     sum = sqrt(n1*n1 + n2*n2)
    ! end function


    ! integer:: ans
    ! ans = get_sum2(5, 4)
    ! print *, ans
    ! contains 
    ! integer function get_sum(n1, n2)
    !     implicit NONE
    !     integer :: n1, n2, sum 
    !     sum = n1 + n2 
    ! end function
    ! function get_sum2(n1, n2) result(sum)
    !     implicit NONE
    !     integer :: n1, n2 
    !     integer :: sum 
    !     sum = n1+n2
    ! end function

END

! REAL FUNCTION F(x, y)
!     IMPLICIT NONE
!     REAL:: x, y
!     F = sqrt(x*x + y*y)
! END FUNCTION