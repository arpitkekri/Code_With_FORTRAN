PROGRAM array
    IMPLICIT NONE

    ! integer:: i, j

    ! real :: x(10)
    ! PRINT *, (X(I), I = 1, 10)

    ! real:: a(2, 2)
    ! READ *,((A(I, J), i = 1, 2), j = 1, 2)
    ! print *, a(1, 2)

    ! real, Dimension(10):: A(10), B(10), C(10), D(10)
    ! A = 0.0
    ! B = C+D 
    ! A(1) = 0.0 
    ! B = 0.0
    ! B = cos(A) 
    ! PRINT *, (b(I), I = 1, 10)

    ! real, Dimension(3, 3):: A, b, c
    ! open(unit=10,file='input.txt')
    ! read(10, *) a
    ! do i = 1, 3
    !     print *,  A(i, 1), A(i, 2), A(i, 3)
    ! enddo
    ! Close (10)

    ! INTEGER, dimension(2,3):: a
    ! do i = 1, 2
    !     do j = 1, 3
    !         a(i,j) = i+2*j
    !     enddo
    ! enddo
    ! PRINT*, 'Array element =', a(2, 3)
    ! PRINT*, 'Array section =', a(:, 3)
    ! PRINT*, 'Sub-array =', a(1:2,2:3)
    ! PRINT *, 'Whole Array =', a
    ! PRINT*, 'Array Transp”d =',TRANSPOSE(a)
    ! do i = 1, 2
    !     print *,(a(i,j), j=1, 3)
    ! enddo

    ! INTEGER, PARAMETER :: a(6)=(/1,2,3,6,7,8/)
    ! REAL :: b(2)=(/SIN(0.2), COS(0.2)/)
    ! INTEGER :: c(5)=(/ 0, a(1:3), 4/)
    ! REAL :: d(100) = (/ (REAL(i),i=1,100)/)
    ! print *, a
    ! print *, b
    ! print *, c 
    ! print *, d


END